{# Karta u výsledku hledání = stejná shrnutí+deník jako plná karta, bez formuláře #}
<div id="work-spool-karta" class="work-spool-card spool-karta-embed">
    <h2 class="spool-karta-embed__title" style="font-size: 1.1rem; margin: 0 0 0.65rem;">Karta cívky: {{ spool.reelNumber|e }}</h2>
    {% include 'warehouse/spool/_spool_typ_under_title.html.twig' %}
    {% include 'warehouse/spool/_spool_summary_and_diary.html.twig' %}
    <p style="margin-top: 0.9rem; margin-bottom: 0;">
        <a href="{{ path('warehouse_spool_show', {id: spool.id}) }}">Otevřít plnou kartu (nová událost v deníku) →</a>
    </p>
</div>
