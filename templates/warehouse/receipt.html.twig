{% extends 'base.html.twig' %}
{% block title %}Příjem kabelu — Optický sklad{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {% include 'warehouse/spool/_skladova_karta_print_queue_styles.html.twig' %}
    <style>
        .receipt-cta {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.65rem 0.75rem;
            max-width: 44rem;
            margin: 0.4rem 0 1.1rem;
            align-items: stretch;
        }
        .receipt-cta__item { display: flex; flex-direction: column; align-items: stretch; gap: 0.2rem; min-width: 0; }
        .receipt-cta__item a.warehouse-pill { width: 100%; text-align: center; }
        .receipt-cta__hint { margin: 0; font-size: 0.78rem; line-height: 1.35; color: #555; padding: 0 0.1rem; }
        .receipt-sublinks { font-size: 0.86rem; line-height: 1.5; color: #444; margin: 0.45rem 0; }
        @media (max-width: 22rem) {
            .receipt-cta { grid-template-columns: 1fr; }
        }
    </style>
{% endblock %}
{% block body %}
    {% include '_app_menu_cz.html.twig' %}
    <h1>Příjem kabelu</h1>
    {% include 'warehouse/spool/_skladova_karta_print_queue_banner.html.twig' %}
    <p class="lede">Zadání <strong>typu kabelu do katalogu</strong> a <strong>nové cívky do evidence</strong> — rozbalené (saře + PS) i nerozbalené (jen typ + L z dokladu).</p>
    <div class="receipt-cta" role="region" aria-label="Hlavní akce">
        <div class="receipt-cta__item">
            <a class="warehouse-pill" href="{{ path('warehouse_spool_new') }}">Nová cívka do evidence</a>
            <p class="receipt-cta__hint">rozbalená špulka: číslo saře, délka L, PS (m₀) a typ kabelu.</p>
        </div>
        {% if is_granted('ROLE_SKLAD_EDIT') %}
        <div class="receipt-cta__item">
            <a class="warehouse-pill" href="{{ path('warehouse_spool_receive_sealed') }}">Příjem nerozbalené</a>
            <p class="receipt-cta__hint">ještě v obalu: typ + L dle dokladu (saře volitelně), PS až po rozbalení.</p>
        </div>
        {% endif %}
        <div class="receipt-cta__item">
            <a class="warehouse-pill" href="{{ path('warehouse_cable_type_new') }}">Nový typ kabelu v katalogu</a>
            <p class="receipt-cta__hint">dříve v aplikaci neexistující značka / konstrukce.</p>
        </div>
    </div>
    <p class="receipt-sublinks"><a href="{{ path('warehouse_cable_type_index') }}">Seznam a úprava typů v katalogu</a> — doplňování parametrů u již vytvořených kódů.</p>
    <p class="receipt-sublinks"><a href="{{ path('warehouse_browse_index') }}">Přehled skladu (filtry)</a> · <a href="{{ path('warehouse_browse_inventura_ucet') }}">Inventura skladový účet</a></p>
    {% include 'warehouse/spool/_skladova_karta_print_queue_footer.html.twig' %}
{% endblock %}
