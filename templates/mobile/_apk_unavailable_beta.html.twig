<div class="mobile-apk-notice" role="status">
    <p class="mobile-apk-notice__lead">
        <strong>Android aplikace (APK)</strong> se na beta verzi (<code>lowpartners.net</code>) nestahuje.
    </p>
    <p>
        APK je pouze na produkčním webu. Otevřete v prohlížeči (ne přes tlačítko „stáhnout“ na této stránce):
    </p>
    <p class="mobile-apk-notice__url"><code>{{ mobile_apk_prod_page_url() }}</code></p>
    <p class="mobile-apk-notice__hint">
        Na produkci teprve zvolte <em>Stáhnout Optický sklad (APK)</em>. Mobilní aplikace z produkce se připojuje k
        <code>optica.lowpartners.net</code>, ne k beta datům.
    </p>
</div>
