# Zkopírovat na server jako:  /home/httpd/html/optica.lowpartners.net/.env.local
# chmod 600 .env.local

### Produkční instance Optický sklad (optica.lowpartners.net)
APP_ENV=prod

# Ostatní tajemství a DATABASE_URL patří do .env.prod.local (Symfony je načte po .env.prod).
