{
  "name": "optica-sklad-android",
  "private": true,
  "version": "0.1.0",
  "description": "Android WebView shell for Optický sklad (Capacitor)",
  "scripts": {
    "cap:sync": "cap sync android && bash scripts/patch-android-manifest.sh && bash scripts/patch-file-paths.sh",
    "cap:open": "cap open android",
    "android:sdk": "bash scripts/write-local-properties.sh",
    "android:assembleDebug": "bash scripts/write-local-properties.sh && cd android && ./gradlew assembleDebug",
    "android:apk": "npm run cap:sync && npm run android:assembleDebug",
    "icons:generate": "node scripts/generate-app-icons.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "sharp": "^0.33.5"
  }
}
